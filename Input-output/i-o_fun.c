#include<stdio.h>

/*
getchar() reads a single character from standard input and putchar() outputs 
a single character at a time to the standard output.
*/

main()
{

char ch;
ch=getchar();
putchar(ch);
}
